services:
  - type: web
    name: supabase-angular
    env: static
    plan: free
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/supabase-angular/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
